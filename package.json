{
  "name": "assemblebot",
  "description": "Bot to listen to and respond to issues open on assemble repositories.",
  "version": "0.1.0",
  "homepage": "https://github.com/assemble/assemblebot",
  "author": "Brian Woodward (https://github.com/doowb)",
  "repository": "assemble/assemblebot",
  "bugs": {
    "url": "https://github.com/assemble/assemblebot/issues"
  },
  "license": "MIT",
  "files": [
    "index.js",
    "dist/",
    "lib/"
  ],
  "main": "index.js",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "build": "browserify index.js -t brfs -g uglifyify --exclude request --exclude async --node --standalone assemblebot -o ./dist/main.js",
    "test": "mocha"
  },
  "dependencies": {
    "async": "^1.5.0",
    "engine": "^0.1.10",
    "github-base": "^0.4.0",
    "github-content": "^0.1.0"
  },
  "devDependencies": {
    "brfs": "^1.4.1",
    "browserify": "^12.0.1",
    "expand-args": "^0.3.0",
    "gulp": "^3.9.0",
    "minimist": "^1.2.0",
    "mocha": "*",
    "should": "*",
    "through2": "^2.0.0",
    "uglifyify": "^3.0.1"
  },
  "keywords": [],
  "verb": {
    "related": {
      "list": [
        "assemble"
      ]
    }
  }
}
